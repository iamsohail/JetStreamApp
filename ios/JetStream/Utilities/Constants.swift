import Foundation

enum Constants {
    static let appName = "JetStream"
    static let bundleId = "com.iamsohail.JetStream"

    enum DeepLinks {
        static let scheme = "jetstream"
        static let flightDetail = "flight"
    }

    enum Defaults {
        static let pageSize = 20
        static let maxPNRLength = 10
    }
}
